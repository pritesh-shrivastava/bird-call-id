# Design: Bird Call Identification PWA — Fine-Tuning Portfolio Piece

Generated by /office-hours on 2026-04-27
Branch: unknown
Repo: priteshshrivastava (home session)
Status: APPROVED
Mode: Builder

## Problem Statement

Build a bird call identification app (web + mobile PWA) focused on **bird species found
in India**. The app listens to a live recording and identifies the species in real time.
The primary goal is a resume-quality fine-tuning showcase: a fine-tuned audio ML model
deployed as a working product, not just a Kaggle notebook.

## What Makes This Cool

The gap in the space: hundreds of BirdCLEF notebooks exist on Kaggle. Almost none are
deployed as real products. The "whoa" is double:

1. **For users:** Hold up your phone, hear a bird, tap record, get "American Robin" in
   3 seconds. Shazam but for birds.
2. **For interviewers:** "My fine-tuned model runs in the browser, offline, with no
   backend." That combination — fine-tuning + ONNX export + client-side inference — is
   genuinely rare in ML portfolios.

## Constraints

- Solo builder, weekends only (roughly 3 weekend sprints)
- Resume impact is primary goal — a shareable link + GitHub repo + documented approach
- No backend hosting budget (free tier acceptable, prefer zero-server for final product)
- **BirdCLEF 2024** dataset as fine-tuning target — specifically designed for birds of the
  Western Ghats of India (~182 species), publicly available on Kaggle, with real competition baselines

## Premises

1. The resume value is in fine-tuning + deployment together, not either alone.
2. BirdCLEF 2024 is the right dataset target: Western Ghats of India, 182 species,
   ~60k training clips, active competition with public baselines to reference.
3. The differentiator vs. 1000 other Kaggle notebooks is end-to-end deployment.
4. A working demo someone can open and use matters more than SOTA accuracy for resume impact.
5. In-browser ONNX inference is worth building toward: eliminates hosting, works offline,
   makes a compelling technical story.

## Approaches Considered

### Approach A: Classic Backend (FastAPI + PyTorch)
- Fine-tune EfficientNet/BirdNET on BirdCLEF, serve via FastAPI, React PWA sends audio
  to API.
- Effort: M (1-2 weekends). Risk: Low.
- Pros: Standard ML engineering pattern, easy model iteration.
- Cons: Needs hosting, latency on mobile, cold start issues.

### Approach B: In-Browser ONNX Inference
- Fine-tune in Python, export to ONNX, run inference in browser via onnxruntime-web.
  WebAudio API for recording, mel spectrogram computed in JS.
- Effort: L (2-3 weekends). Risk: Med.
- Pros: "Runs offline in-browser" is a rare, impressive portfolio story.
- Cons: ONNX export can be finicky; browser audio pipeline is non-trivial.

### Approach C: Hugging Face Spaces (Zero-Infra Demo)
- Fine-tune model, push to HF Hub, deploy Gradio demo on Spaces. PWA wrapper calls
  HF Inference API.
- Effort: S (1 weekend). Risk: Low.
- Pros: Shareable link in hours, model card is itself a portfolio artifact.
- Cons: Less technically impressive; limited UX control.

## Recommended Approach

**Phase 1 (Weekend 1): Approach C — HF Spaces**
Validate that the fine-tuned model actually works before investing in the ONNX pipeline.
Goals: run BirdCLEF baseline training, fine-tune EfficientNet-B0 or Perch on BirdCLEF 2025
data, push to Hugging Face Hub, deploy Gradio demo on Spaces. Deliverable: a shareable
URL that identifies bird calls from an uploaded audio file.

**Phase 2 (Weekends 2-3): Approach B — ONNX PWA**
Export the validated model to ONNX, build a React PWA with WebAudio API recording and
onnxruntime-web inference. Deliverable: a mobile-installable PWA that identifies birds
in real time, no server required.

### Suggested Tech Stack

**Training:**
- Dataset: **BirdCLEF 2024** (Kaggle: `birdclef-2024`, Western Ghats of India focus)
- Target species: all 182 Western Ghats species (no filtering needed — the dataset
  is already scoped to this region; fits within Kaggle free GPU quota ~30h/week)
- Base model: **EfficientNet-B0 on mel spectrograms** (recommended for v1 — simple,
  well-documented, straightforward ONNX export). Google Perch is more impressive to
  name-drop but requires TF/JAX and adds ~1 weekend of integration risk; defer to v2.
- Framework: PyTorch + torchaudio
- Training environment: Kaggle Notebooks (free GPU, dataset co-located). Google Colab
  as fallback if weekly quota runs out.

**Preprocessing spec (must match between training and browser):**
- Sample rate: 32000 Hz
- Audio clip length: 5 seconds (BirdCLEF standard)
- Mel spectrogram: n_mels=128, hop_length=320, n_fft=1024, fmin=20, fmax=16000
- Output shape: (128, 501) float32 spectrogram fed to EfficientNet-B0
- These values must be reproduced exactly in the browser. Mismatch = silent accuracy
  degradation. Use meyda.js for browser-side mel computation and validate its output
  against librosa on 5 test clips before trusting Phase 2 inference.

**Phase 1 serving:**
- HF Hub (model weights + model card)
- Gradio demo deployed to HF Spaces (free tier)

**Phase 2 app:**
- React + Vite PWA
- onnxruntime-web for in-browser inference
- WebAudio API for live recording
- mel spectrogram computed in JS (or via WASM utility)
- Deployed to Vercel or Cloudflare Pages (static, free)

## Open Questions

1. Model size: EfficientNet-B0 exported to ONNX is ~20-25MB. First inference on a
   mid-range Android may exceed 3s. Validate mobile benchmark early; quantize to int8
   (~6MB) if needed. Target latency is 5s on mid-range Android for v1 (3s is aspirational).
2. Recording interaction model: press-and-hold for 5 seconds, then inference on release.
   Do NOT attempt sliding-window real-time inference for v1 — significantly higher
   complexity with no user-visible benefit at this stage.
3. ONNX export parity: after exporting to ONNX, top-1 predictions must match PyTorch
   on >99% of the 50-species validation set before proceeding to Phase 2.
4. Audio preprocessing in browser: use meyda.js for mel spectrogram computation. Validate
   meyda.js output against librosa on at least 5 test clips before trusting inference.

## Success Criteria

- Phase 1: A URL someone can open that correctly identifies Western Ghats bird species from
  uploaded audio. Model achieves cmAP >= 0.65 on the BirdCLEF 2024 public validation set
  (the competition baseline, 182 species). Model card on HF Hub with architecture, dataset, and score.
- Phase 2: A PWA installable on mobile. Press-and-hold records 5s, inference completes
  within 5s on mid-range Android, no backend call during inference. Low-confidence state
  (< 0.4) shows "no bird detected" instead of a wrong ID.
- Portfolio artifact: GitHub repo with clean README, training notebook, model card,
  preprocessing spec, and live demo link.

## Distribution Plan

- Phase 1: Hugging Face Spaces (auto-deploy from HF Hub push)
- Phase 2: Vercel or Cloudflare Pages (static PWA, deploy on git push)
- GitHub repo: README with demo GIF, training details, model architecture diagram,
  preprocessing spec, results vs. baseline. This is the artifact interviewers will read.

## Next Steps

1. Set up Kaggle notebook, download BirdCLEF 2025 dataset, run the existing baseline
   EfficientNet-B0 training to verify the environment works.
2. Fine-tune for 5-10 epochs on the full dataset or a 50-species subset. Save checkpoint.
3. Push checkpoint to Hugging Face Hub. Write model card (architecture, dataset, eval score).
4. Build Gradio demo on HF Spaces. Test it.
5. Export model to ONNX. Verify inference parity with PyTorch.
6. Build React PWA with WebAudio recording + onnxruntime-web.
7. Deploy to Vercel. Test on mobile.
8. Write the README. Add demo GIF. Post to LinkedIn.

## The Assignment

This week: open Kaggle, find the BirdCLEF 2024 competition page (`birdclef-2024`), fork
a starter notebook, and run it to completion to verify you can get a model checkpoint out.
Don't worry about fine-tuning yet. Just prove the training loop works and you understand
the data shape (182 Western Ghats species, ~60k clips, 32kHz).
That's it. One checkpoint.

## What I noticed about how you think

- When you heard "in-browser ONNX inference," you immediately wanted to explore it.
  You didn't ask "is this necessary?" You asked what it was. That instinct toward the
  harder, more interesting solution is the right one for a portfolio piece.
- You said "bird classifier looked like a finetuning use case but I guess there are many
  out there." That doubt is healthy but slightly misdirected. There are many fine-tuned
  classifiers. There are very few deployed products built on top of them. You're building
  the second thing, not the first.
- You asked to create a folder before we finished the design. Builder energy. Just finish
  the map first, then start moving.
